# -----------------------------------------------------------------------------
# Environment template
# Copy this file to `.env` and replace placeholder values.
# -----------------------------------------------------------------------------

# === OpenAI ================================================================
# Create a key at https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-key

# Model to use for generation
# - gpt-4o-mini : fast & cost-effective
# - gpt-4o      : higher quality, higher cost
MODEL=gpt-4o-mini

# Upper bound on tokens generated by the model (use this to controls article length/cost)
MAX_TOKENS=1800


# === Database (Supabase / Postgres) ========================================
# OPTION A (recommended): Provide raw components.
# This avoids URL-encoding issues with special characters in the password.
SUPABASE_USER=postgres
SUPABASE_PASSWORD=your-postgres-password
SUPABASE_HOST=db.xxxxxx.supabase.co
SUPABASE_DB=postgres
SUPABASE_PORT=5432

# OPTION B (advanced): Single DSN string (leave blank if using Option A).
# If you use this, make sure PASSWORD is URL-encoded (e.g., @ -> %40).
# Example: postgresql://USER:PCTENCODED_PASSWORD@HOST:5432/DB
SUPABASE_DB_URL=

# Notes:
# - The app will prefer Option A if both are set.
# - SSL is required when connecting to Supabase
